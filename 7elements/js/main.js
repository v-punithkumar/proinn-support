jQuery(document).ready(function ($) {

    $('.accordion').accordion({
        autoHeight: false,
        collapsible: false,
        heightStyle: 'content',
        active: 0,
    });

    $('.tabs').tabs();

});
